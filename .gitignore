venv/
__pycache__/
*.pyc

# Real downloaded SROIE data (huge, and network-restricted to download
# anyway — see README for the real download step)
data/raw/sroie/*
!data/raw/sroie/.gitkeep

# Generated/derived data artifacts — regenerated by running the pipeline
data/processed/receipts.json
data/synthetic/*.json

# Evaluation outputs — regenerated by re-running the evaluation scripts
results/*.json

.DS_Store


